'use strict'

const curry = require('fj-curry').curry

const has = curry((properties, object) => {
  try {
    return properties.every((property) => property in object)
  } catch (exception) {
    // When the 'in' operator can't be used (non-objects)
    return false
  }
})

module.exports = has
