'use strict'

const fs = require('fs')
const path = require('path')

fs.readdirSync(__dirname).forEach((name) => {
  if (name !== path.basename(__filename)) {
    exports[name.slice(0, -3)] = require(path.join(__dirname, name))
  }
})
