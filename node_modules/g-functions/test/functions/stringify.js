'use strict'

const assert = require('assert')
const G = require('../../lib')

describe('stringify', function () {
  it('value', function () {
    const value = [1, 2, 3]
    assert.equal(G.stringify(null, 2, value), JSON.stringify(value, null, 2))
  })
})
