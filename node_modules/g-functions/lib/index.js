'use strict'

const fs = require('fs')
const path = require('path')

const lib = path.join(__dirname, 'functions')

fs.readdirSync(lib).forEach((name) => {
  exports[path.basename(name, '.js')] = require(path.join(lib, name))
})
