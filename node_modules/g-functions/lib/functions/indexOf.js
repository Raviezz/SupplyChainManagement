'use strict'

const generic = require('@nodeguy/generic')
const _ = generic._
const is = require('@nodeguy/type').is

const indexOf = generic.function({name: 'indexOf', curry: true})

// https://www.ecma-international.org/ecma-262/6.0/#sec-string.prototype.indexof
indexOf.method([_, _, is(String)],
  (searchString, position, string) =>
    string.indexOf(searchString, position)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-array.prototype.indexof
indexOf.method([_, _, is(Array)],
  (searchElement, fromIndex, array) =>
    array.indexOf(searchElement, fromIndex)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.indexof
indexOf.method([_, _, is(Int8Array)],
  (searchElement, fromIndex, int8Array) =>
    int8Array.indexOf(searchElement, fromIndex)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.indexof
indexOf.method([_, _, is(Uint8Array)],
  (searchElement, fromIndex, uint8Array) =>
    uint8Array.indexOf(searchElement, fromIndex)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.indexof
indexOf.method([_, _, is(Uint8ClampedArray)],
  (searchElement, fromIndex, uint8ClampedArray) =>
    uint8ClampedArray.indexOf(searchElement, fromIndex)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.indexof
indexOf.method([_, _, is(Int16Array)],
  (searchElement, fromIndex, int16Array) =>
    int16Array.indexOf(searchElement, fromIndex)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.indexof
indexOf.method([_, _, is(Uint16Array)],
  (searchElement, fromIndex, uint16Array) =>
    uint16Array.indexOf(searchElement, fromIndex)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.indexof
indexOf.method([_, _, is(Int32Array)],
  (searchElement, fromIndex, int32Array) =>
    int32Array.indexOf(searchElement, fromIndex)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.indexof
indexOf.method([_, _, is(Uint32Array)],
  (searchElement, fromIndex, uint32Array) =>
    uint32Array.indexOf(searchElement, fromIndex)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.indexof
indexOf.method([_, _, is(Float32Array)],
  (searchElement, fromIndex, float32Array) =>
    float32Array.indexOf(searchElement, fromIndex)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.indexof
indexOf.method([_, _, is(Float64Array)],
  (searchElement, fromIndex, float64Array) =>
    float64Array.indexOf(searchElement, fromIndex)
)

module.exports = indexOf
