'use strict'

const generic = require('@nodeguy/generic')
const _ = generic._
const is = require('@nodeguy/type').is

const copyWithin = generic.function({name: 'copyWithin', curry: true})

// https://www.ecma-international.org/ecma-262/6.0/#sec-array.prototype.copywithin
copyWithin.method([_, _, _, is(Array)],
  (target, start, end, array) =>
    array.copyWithin(target, start, end)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.copywithin
copyWithin.method([_, _, _, is(Int8Array)],
  (target, start, end, int8Array) =>
    int8Array.copyWithin(target, start, end)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.copywithin
copyWithin.method([_, _, _, is(Uint8Array)],
  (target, start, end, uint8Array) =>
    uint8Array.copyWithin(target, start, end)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.copywithin
copyWithin.method([_, _, _, is(Uint8ClampedArray)],
  (target, start, end, uint8ClampedArray) =>
    uint8ClampedArray.copyWithin(target, start, end)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.copywithin
copyWithin.method([_, _, _, is(Int16Array)],
  (target, start, end, int16Array) =>
    int16Array.copyWithin(target, start, end)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.copywithin
copyWithin.method([_, _, _, is(Uint16Array)],
  (target, start, end, uint16Array) =>
    uint16Array.copyWithin(target, start, end)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.copywithin
copyWithin.method([_, _, _, is(Int32Array)],
  (target, start, end, int32Array) =>
    int32Array.copyWithin(target, start, end)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.copywithin
copyWithin.method([_, _, _, is(Uint32Array)],
  (target, start, end, uint32Array) =>
    uint32Array.copyWithin(target, start, end)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.copywithin
copyWithin.method([_, _, _, is(Float32Array)],
  (target, start, end, float32Array) =>
    float32Array.copyWithin(target, start, end)
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.copywithin
copyWithin.method([_, _, _, is(Float64Array)],
  (target, start, end, float64Array) =>
    float64Array.copyWithin(target, start, end)
)

module.exports = copyWithin
