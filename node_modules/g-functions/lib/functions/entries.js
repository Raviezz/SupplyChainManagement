'use strict'

const generic = require('@nodeguy/generic')
const is = require('@nodeguy/type').is

const entries = generic.function({name: 'entries', curry: true})

// https://www.ecma-international.org/ecma-262/6.0/#sec-array.prototype.entries
entries.method([is(Array)],
  (array) =>
    array.entries()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.entries
entries.method([is(Int8Array)],
  (int8Array) =>
    int8Array.entries()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.entries
entries.method([is(Uint8Array)],
  (uint8Array) =>
    uint8Array.entries()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.entries
entries.method([is(Uint8ClampedArray)],
  (uint8ClampedArray) =>
    uint8ClampedArray.entries()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.entries
entries.method([is(Int16Array)],
  (int16Array) =>
    int16Array.entries()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.entries
entries.method([is(Uint16Array)],
  (uint16Array) =>
    uint16Array.entries()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.entries
entries.method([is(Int32Array)],
  (int32Array) =>
    int32Array.entries()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.entries
entries.method([is(Uint32Array)],
  (uint32Array) =>
    uint32Array.entries()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.entries
entries.method([is(Float32Array)],
  (float32Array) =>
    float32Array.entries()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.entries
entries.method([is(Float64Array)],
  (float64Array) =>
    float64Array.entries()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-map.prototype.entries
entries.method([is(Map)],
  (map) =>
    map.entries()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-set.prototype.entries
entries.method([is(Set)],
  (set) =>
    set.entries()
)

module.exports = entries
