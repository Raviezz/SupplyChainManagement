'use strict'

const generic = require('@nodeguy/generic')
const is = require('@nodeguy/type').is

const keys = generic.function({name: 'keys', curry: true})

// https://www.ecma-international.org/ecma-262/6.0/#sec-array.prototype.keys
keys.method([is(Array)],
  (array) =>
    array.keys()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.keys
keys.method([is(Int8Array)],
  (int8Array) =>
    int8Array.keys()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.keys
keys.method([is(Uint8Array)],
  (uint8Array) =>
    uint8Array.keys()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.keys
keys.method([is(Uint8ClampedArray)],
  (uint8ClampedArray) =>
    uint8ClampedArray.keys()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.keys
keys.method([is(Int16Array)],
  (int16Array) =>
    int16Array.keys()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.keys
keys.method([is(Uint16Array)],
  (uint16Array) =>
    uint16Array.keys()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.keys
keys.method([is(Int32Array)],
  (int32Array) =>
    int32Array.keys()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.keys
keys.method([is(Uint32Array)],
  (uint32Array) =>
    uint32Array.keys()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.keys
keys.method([is(Float32Array)],
  (float32Array) =>
    float32Array.keys()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-%typedarray%.prototype.keys
keys.method([is(Float64Array)],
  (float64Array) =>
    float64Array.keys()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-map.prototype.keys
keys.method([is(Map)],
  (map) =>
    map.keys()
)

// https://www.ecma-international.org/ecma-262/6.0/#sec-set.prototype.keys
keys.method([is(Set)],
  (set) =>
    set.keys()
)

module.exports = keys
