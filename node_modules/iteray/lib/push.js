'use strict'

const generic = require('@nodeguy/generic')
const _ = generic._
const has = require('@nodeguy/type').has

const push = generic.create({name: 'push', curry: true})

push.method([has(['push']), _],
  (object, element) => {
    object.push(element)
  }
)

module.exports = push
