'use strict'

const assert = require('assert')
const I = require('../lib')

it('pushes an element onto the end of an array', function () {
  const elements = [1, 2]
  I.push(elements, 3)
  assert.deepEqual(elements, [1, 2, 3])
})
